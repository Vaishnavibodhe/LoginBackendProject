<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full px-3 h-screen bg-zinc-900 text-white">
        <div class=" w-full p-2 flex justify-end">
        <a class="w-30 bg-red-500 rounded-md inline-block">Logout</a>
    </div>
    <div class="flex item-start gap-3">
    <div class="w-10 h-10 rounded-md overflow-hidden" >
        <img class="w-full h-screen object-cover "src="/images/<%=user.profilepic%>">
         </div>
        <h3 class="text-3xl mb-5">Hello,<%= user.email %></h3>
    </div>
        <h5 class="text-2xl m-2">You can create a new post</h5>
    
<form action="/post" method="post">
   <textArea name="content" placeholder=" Whats on your mind" class="w-1/3 outline-none p-3 resize-none bg-transparent border-2">Whats On your mind</textArea>
        <input type="submit"value="create new content " class="w-30 py-2 bg-blue-500 border-2 rounded-md mt-2">
</form>
<div class="posts mt-20">
<h3 class=" text-zinc-400">Your posts.</h3>
<div class="postcontainer mt-5">
    <% user.posts.forEach(function(post){ %>
    <div class="post w-1/3 p-3 border-2 border-zinc-800">
        <h4 class=" text-blue-500">@<%=user.username %></h4>
        <p class="text-sm tracking-tighter"><%=post.content %></p>
<small class=" text-white inline-block"><%= post.likes.length %> likes</small>
    <div class="btns flex">
    <a class=" text-blue-500 m-2" href="/like/<%=post._id %>">
        <%= post.likes.indexOf(user._id) ? "like" : "unLike" %>

    </a>
    <a class=" text-zinc-400 m-2" href="/edit/<%=post._id%>">edit</a>
    </div>
</div>
<% }) %>
</div>
    </div>
</body>
</html>